{
  "name": "reqto",
  "version": "1.0.0",
  "description": "Promise based HTTP client for the browser",
  "private": false,
  "main": "src/reqTo.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack",
    "start:dev": "webpack-dev-server --output-public-path=/dist/ --progress --hot",
    "build:prod": "node_modules/.bin/webpack --mode production",
    "serve": "webpack-dev-server --mode development --hot -w --progress --output-public-path=/dist/"

  },
  "keywords": [
    "reqto",
    "http",
    "ajax",
    "promise"
  ],
  "author": "Gaurav Palaspagar",
  "license": "ISC",
  "dependencies": {

  },
  "devDependencies": {

  }
}